<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="My_Pop_Up" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:background">#333333</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:textColorPrimary">#000000</item>
        <item name="android:colorForeground">#000000</item>
        <item name="colorControlNormal">#000000</item>
    </style>

    <style name="Main_Popup" parent="@android:style/Widget.PopupMenu">
        <item name="android:textColor">#000000</item>
        <item name="android:textColorPrimary">#ffffff</item>
        <item name="android:colorForeground">#ffffff</item>
        <item name="colorControlNormal">#ffffff</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="MyAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Used for the buttons -->
        <item name="colorAccent">#FFFFFF</item>
        <!-- Used for the title and text -->
        <item name="android:textColorPrimary">#FFFFFF</item>
        <!-- Used for the background -->
        <item name="android:background">#CC000000</item>
    </style>

    <style name="myDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@color/sy_softpink</item>
        <item name="android:textColor">@color/sy_brown</item>
        <item name="android:textColorPrimary">@color/sy_brown</item>
        <item name="android:textColorHighlight">@color/sy_brown</item>
        <item name="colorAccent">@color/sy_brown</item>
    </style>



</resources>
